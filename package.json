{
  "name": "ndcautomation",
  "version": "1.0.0",
  "description": "NDC Automation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browser:chrome": "npx cypress run --browser=chrome --headed --spec cypress/e2e/Create_Suppliers.cy.js --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "author": "KarthickKR",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.29.0",
    "allure-cypress": "^3.0.0-beta.7",
    "cypress": "^13.13.2",
    "mocha-allure-reporter": "^1.4.0"
  },
  "dependencies": {
    "ndcautomation": "file:",
    "ui-automation": "file:"
  }
}
